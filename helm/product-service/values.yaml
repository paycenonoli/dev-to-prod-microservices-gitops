replicaCount: 1

imagePullSecrets: []

image:
  repository: product-service
  tag: "1.0.0"
  pullPolicy: IfNotPresent

environment:
  name: dev

service:
  type: ClusterIP
  port: 8081

ingress:
  enabled: false
  className: traefik
  host: ""
  path: /products
  pathType: Prefix
