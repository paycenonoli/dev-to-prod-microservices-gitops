replicaCount: 2

image:
  repository: frontend
  tag: "1.0.0"
  pullPolicy: IfNotPresent

environment:
  name: dev

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true
  className: traefik
  host: ""
  path: /
  pathType: Prefix
